version: '3'
services: 
    postgres:
        image: 'postgres:latest'
        restart: always
        environment: 
             - POSTGRES_PASSWORD 
             - POSTGRES_PORT
        ports:
            - '4000:4000'